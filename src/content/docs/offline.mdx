---
title: AIPali 智能化巴利三藏
description: 本地数据控制中心，管理离线法宝数据。
template: splash
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import heroLight from '../../assets/aipali-hero-visual-light.webp';
import heroDark from '../../assets/aipali-hero-visual-dark.webp';

{/* ====================================================== */}
{/* 0. 路由守卫：非 Github/Local 强制跳转 */}
{/* ====================================================== */}
<script is:inline>{`
  (function() {
    const hostname = window.location.hostname;
    const isAllowed = hostname.includes('github.io') || 
                      hostname === 'localhost' || 
                      hostname === '127.0.0.1' || 
                      hostname.startsWith('192.168.');
    
    if (!isAllowed) {
      window.location.replace('https://aipali.github.io/offline/');
    }
  })();
`}</script>

{/* ====================================================== */}
{/* 1. 原有首页 Hero */}
{/* ====================================================== */}
<section className="custom-hero-section">
  <div className="hero-container">
    <div className="hero-text">
      <h1 className="hero-title">巴利三藏智能化工程</h1>
      <p className="hero-tagline">
        基于现代人工智能技术，重塑法宝的阅读、检索与互动体验。
      </p>
      <div className="hero-actions">
        <a href="/tipitaka/" className="btn btn-primary">阅读法宝 →</a>
        <a href="/info/how-to-use/" className="btn btn-secondary">如何使用</a>
        <a href="/info/about-aipali/" className="btn btn-minimal">关于本工程</a>
      </div>
    </div>

    <div className="hero-image">
      <img src={heroLight.src} alt="AIPali Light" className="img-light" />
      <img src={heroDark.src} alt="AIPali Dark" className="img-dark" />
    </div>
  </div>
</section>

{/* ====================================================== */}
{/* 2. 离线控制中心 (高级玻璃态 UI) */}
{/* ====================================================== */}
<div className="page-container">
  <div className="divider-glow"></div>

  <div className="content-wrapper">
    <h2 className="aipali-section-title" style={{ marginTop: '2rem' }}>本地法宝数据中心</h2>
    <p style={{ textAlign: 'center', color: 'var(--sl-color-gray-2)', marginBottom: '3rem', fontSize: '1.1rem' }}>
      将三藏数据完整同步至设备本地。同步完成后，应用将<strong>永久断开对应网络请求</strong>，以纯单机模式运行，确保阅藏过程的绝对流畅与专注。
    </p>

    {/* 数据仪表盘 - 采用 About.mdx 的玻璃态风格 */}
    <div className="aipali-glass-grid" style={{ marginTop: '0', marginBottom: '2.5rem' }}>
      <div className="aipali-glass-card" style={{ padding: '2rem', textAlign: 'center', alignItems: 'center' }}>
        <h3 style={{ margin: '0', fontSize: '1.1rem', color: 'var(--sl-color-gray-3)' }}>已缓存经文页数</h3>
        <div className="dash-value">
          <span id="cache-count">--</span> <span className="dash-unit" style={{ fontSize: '1rem', fontWeight: 'normal' }}>篇</span>
        </div>
      </div>
      <div className="aipali-glass-card" style={{ padding: '2rem', textAlign: 'center', alignItems: 'center' }}>
        <h3 style={{ margin: '0', fontSize: '1.1rem', color: 'var(--sl-color-gray-3)' }}>本地存储占用</h3>
        <div className="dash-value">
          <span id="storage-usage">--</span> <span className="dash-unit" style={{ fontSize: '1rem', fontWeight: 'normal' }}>MB</span>
        </div>
      </div>
    </div>

    {/* 操作面板 */}
    <div className="control-panel">
      <button id="btn-update" className="btn-action btn-primary-action" onclick="window.startSync(false)">
        增量同步数据
      </button>
      <button id="btn-rebuild" className="btn-action btn-secondary-action" onclick="window.startSync(true)">
        全量重新构建
      </button>
      <button id="btn-clear" className="btn-action btn-danger-action" onclick="window.clearAllCache()">
        清除本地数据
      </button>
    </div>

    {/* 进度条 */}
    <div id="progress-container" className="progress-container hidden">
      <div className="progress-info">
        <span id="progress-text">等待指令...</span>
        <span id="progress-percent">0%</span>
      </div>
      <div className="progress-bar-bg">
        <div id="progress-bar-fill" className="progress-bar-fill"></div>
      </div>
    </div>

    {/* 现代化终端日志窗口 */}
    <div className="terminal-window">
      <div className="terminal-header">
        <div className="window-controls">
          <span className="dot dot-red"></span>
          <span className="dot dot-yellow"></span>
          <span className="dot dot-green"></span>
        </div>
        <span className="terminal-title">AIPali Sync Protocol</span>
      </div>
      <div id="log-window" className="terminal-body">
        <div className="log-line">系统初始化完成，存储引擎已就绪。</div>
      </div>
    </div>
  </div>

  <div className="divider-glow" style={{ marginTop: '5rem' }}></div>

  {/* ====================================================== */}
  {/* 3. 特性卡片与页脚 */}
  {/* ====================================================== */}
  <div className="content-wrapper">
    <CardGrid>
      <Card title="完整法宝收录" icon="document">
        以 <b className="highlight-text">VRI/CSCD</b> 巴利语原文为底稿，涵盖完整的<b className="highlight-text">五部经藏</b>（含小部 15 经）与<b className="highlight-text">律藏</b>（含大小品及附随）；内置<b className="highlight-text">段落级巴利原文对照</b>，助力深度法义探究。
      </Card>
      <Card title="结构化内容梳理" icon="list-format">
        依托大语言模型重新梳理经文的层次，自动生成<b className="highlight-text">多层级标题</b>、<b className="highlight-text">AI导读</b>与<b className="highlight-text">法义解析</b>，在恢复经文本有的工整结构的同时，大幅降低现代修行者阅读理解门槛。
      </Card>
      <Card title="现代化阅读体验" icon="laptop">
        应用<b className="highlight-text">现代互联网技术</b>，提供 PC、手机、平板上的优质阅读体验，并支持<b className="highlight-text">离线浏览</b>；内置<b className="highlight-text">毫秒级全文搜索引擎</b>，可根据经文编号、法义术语直达原文段落。
      </Card>
      <Card title="互动化巴利三藏" icon="rocket">
        以 <b className="highlight-text">AI 检索增强技术</b> 赋能传统经典，构建“可聊天”的巴利三藏。无论是搜寻法义、深入探讨甚至实修实证，AI 助手都能提供详尽支持，并附带<b className="highlight-text">经文出处</b>。
      </Card>
    </CardGrid>
  </div>

  <footer className="aipali-custom-footer">
    <div className="footer-content">
      <p className="footer-slogan">
        <strong className="brand">AIPali 智能化巴利三藏</strong>
        <span className="divider"> | </span> 
        <span className="keyword">结构化</span> <span className="dot"> · </span> 
        <span className="keyword">现代化</span> <span className="dot"> · </span> 
        <span className="keyword">互动化</span>
      </p>
      <p className="footer-sub">由现代人开发，服务于现代佛法修行者</p>
      <p className="footer-copyright">
        &copy; 2026, <a href="https://true-dhamma.com" target="_blank" rel="noopener noreferrer">如实法 True-Dhamma.com</a>
      </p>
    </div>
  </footer>
</div>

{/* ====================================================== */}
{/* 4. 离线控制台 JS 引擎 (修复数据获取延迟与安全提示) */}
{/* ====================================================== */}
<script is:inline>{`
  const CACHE_NAME = 'aipali-offline-cache';
  let isSyncing = false;

  const UI = {
    log: function(msg) {
      const el = document.getElementById('log-window');
      const time = new Date().toLocaleTimeString('en-GB');
      
      const div = document.createElement('div');
      div.className = 'log-line';
      div.textContent = '[' + time + '] ' + msg;
      
      el.appendChild(div);
      
      // 保持最多 60 行，避免内存溢出
      while (el.children.length > 60) {
        el.removeChild(el.firstChild);
      }
      el.scrollTop = el.scrollHeight;
    },
    updateCount: function(count) {
      document.getElementById('cache-count').innerText = count;
    },
    updateStorage: function(usage) {
      document.getElementById('storage-usage').innerText = usage;
    },
    showProgress: function(show) {
      const el = document.getElementById('progress-container');
      if (show) {
        el.classList.remove('hidden');
        void el.offsetWidth;
        el.classList.add('visible');
      } else {
        el.classList.remove('visible');
        setTimeout(function() { el.classList.add('hidden'); }, 300);
      }
    },
    setProgress: function(current, total, text) {
      const percent = total > 0 ? Math.round((current / total) * 100) : 0;
      document.getElementById('progress-bar-fill').style.width = percent + '%';
      document.getElementById('progress-percent').innerText = percent + '%';
      document.getElementById('progress-text').innerText = text;
    },
    disableButtons: function(disabled) {
      document.getElementById('btn-update').disabled = disabled;
      document.getElementById('btn-rebuild').disabled = disabled;
      document.getElementById('btn-clear').disabled = disabled;
    }
  };

  // 刷新状态：增加延时重试以确保读取到最新 Size
  async function refreshStatus() {
    try {
      if ('caches' in window) {
        const hasCache = await caches.has(CACHE_NAME);
        if (hasCache) {
          const cache = await caches.open(CACHE_NAME);
          const keys = await cache.keys();
          // 仅统计经文 HTML 页面，过滤掉被缓存的 css/js/png 等资源文件，防止数字虚高
          const htmlPages = keys.filter(req => req.url.endsWith('/') || req.url.endsWith('.html'));
          UI.updateCount(htmlPages.length);
        } else {
          UI.updateCount(0);
        }
      } else {
        UI.updateCount('N/A');
        UI.log('终端警告: 当前浏览器环境不支持 Cache API。');
      }

      if (navigator.storage && navigator.storage.estimate) {
        const estimate = await navigator.storage.estimate();
        const usedMB = (estimate.usage / (1024 * 1024)).toFixed(2);
        UI.updateStorage(usedMB);
      } else {
        UI.updateStorage('--');
      }
    } catch (e) {
      UI.log('状态读取异常: ' + e.message);
    }
  }

  // 触发刷新并设定一个 1 秒后的二次刷新 (因为浏览器测算硬盘空间有延迟)
  function triggerRefresh() {
    refreshStatus();
    setTimeout(refreshStatus, 1500);
  }

  window.clearAllCache = async function() {
    if (isSyncing) return;
    const msg = '【彻底清除确认】\\n\\n此操作将抹除设备上所有已下载的法宝数据，您的应用将恢复至初始状态，断网时将无法阅读。\\n\\n确定执行清除吗？';
    if (!confirm(msg)) return;
    
    UI.log('正在执行本地区块清除协议...');
    try {
      await caches.delete(CACHE_NAME);
      UI.log('数据已完全抹除，缓存已重置。');
      triggerRefresh();
    } catch (e) {
      UI.log('清除操作失败: ' + e.message);
    }
  };

  window.startSync = async function(isRebuild) {
    if (isSyncing) return;
    
    let msg = isRebuild 
      ? '【全量重建确认】\\n\\n此操作将首先清空所有旧的缓存（包括旧版本的样式文件），然后重新下载全部最新经文。耗时较长。\\n\\n若发现样式异常或体积过大，推荐使用此功能。确定开始吗？'
      : '【增量同步确认】\\n\\n此操作仅下载您设备上缺失的经文，耗时较短。\\n\\n确定开始吗？';
      
    if (!confirm(msg)) return;

    isSyncing = true;
    UI.disableButtons(true);
    UI.showProgress(true);
    
    UI.log('');
    UI.log(isRebuild ? '=== 启动全量重建协议 ===' : '=== 启动增量同步协议 ===');

    try {
      if (!navigator.onLine) {
        throw new Error('网络连接异常，请检查您的设备是否已接入互联网。');
      }

      UI.log('读取服务端结构目录...');
      const sitemapRes = await fetch('/sitemap-0.xml', { cache: 'no-store' });
      if (!sitemapRes.ok) throw new Error('服务器无响应 (HTTP ' + sitemapRes.status + ')');
      
      const sitemapText = await sitemapRes.text();
      const parser = new DOMParser();
      const xml = parser.parseFromString(sitemapText, "text/xml");
      
      const urls = Array.from(xml.querySelectorAll("loc"))
        .map(function(node) {
          try { return new URL(node.textContent).pathname; } 
          catch(e) { return node.textContent; }
        })
        .filter(function(url) {
          // 仅过滤掉 /tags/ 和 /info/，保留 / (Index) 和 /offline/ 确保核心路由被缓存
          return !url.includes('/tags/');
        });

      if (urls.length === 0) throw new Error('未获取到合法数据节点。');
      UI.log('校验通过，需保障 ' + urls.length + ' 个核心节点。');

      const cache = await caches.open(CACHE_NAME);
      let existingKeys = [];
      
      if (!isRebuild) {
        const reqs = await cache.keys();
        existingKeys = reqs.map(function(req) { return new URL(req.url).pathname; });
        UI.log('已提取本地特征码，开始差异比对...');
      } else {
        UI.log('正在格式化旧缓存，释放存储空间...');
        await caches.delete(CACHE_NAME);
        // 需重新打开一个干净的缓存池
        await caches.open(CACHE_NAME);
      }

      let downloaded = 0;
      let skipped = 0;
      let errors = 0;
      let currentIndex = 0;
      const MAX_CONCURRENT = 5;

      const downloadWorker = async function() {
        while (currentIndex < urls.length) {
          const index = currentIndex++;
          const url = urls[index];
          const displayUrl = url === '/' ? '首页 (Index)' : url.split('/').filter(Boolean).slice(-2).join('/'); 

          if (!isRebuild && existingKeys.includes(url)) {
            skipped++;
          } else {
            try {
              UI.setProgress(currentIndex, urls.length, '同步中: ' + displayUrl);
              const res = await fetch(url, { cache: 'no-cache' });
              
              if (res.ok) {
                // 必须重新获取 cache 实例，防止异步上下文丢失
                const currentCache = await caches.open(CACHE_NAME);
                await currentCache.put(url, res);
                downloaded++;
              } else {
                UI.log('节点响应失效 [' + res.status + ']: ' + displayUrl);
                errors++;
              }
            } catch (err) {
              UI.log('网络传输阻断: ' + displayUrl);
              errors++;
            }
          }
          
          if (currentIndex % 10 === 0 || currentIndex >= urls.length - MAX_CONCURRENT) {
            UI.setProgress(currentIndex, urls.length, '总进度: ' + currentIndex + ' / ' + urls.length);
          }
        }
      };

      UI.log('开启并发传输通道 (Thread Pool: 5)...');
      const workers = Array(Math.min(MAX_CONCURRENT, urls.length)).fill(null).map(function() { return downloadWorker(); });
      await Promise.all(workers);

      UI.setProgress(urls.length, urls.length, '数据同步已完成');
      UI.log('操作完成 | 新建更新: ' + downloaded + ' | 跳过: ' + skipped + ' | 异常: ' + errors);

    } catch (e) {
      if (e.message.includes('Failed to fetch') || e.message.includes('NetworkError')) {
        UI.log('同步中止: 网络无法连接，请检查设备网络状态。');
      } else {
        UI.log('同步中止: ' + e.message);
      }
    } finally {
      isSyncing = false;
      UI.disableButtons(false);
      triggerRefresh();
      setTimeout(function() { UI.showProgress(false); }, 3000);
    }
  };

  document.addEventListener('DOMContentLoaded', function() {
    refreshStatus();
    window.addEventListener('online', function() { UI.log('网络连接已重构，同步功能可用。'); });
    window.addEventListener('offline', function() { UI.log('网络中断，系统已平滑切换至本地数据池。'); });
  });
`}</script>

{/* ====================================================== */}
{/* 5. 样式表 (全量适配日间/夜间模式) */}
{/* ====================================================== */}
<style>{`
  .sl-markdown-content > h1:first-child, #_top { display: none !important; }
  .sl-markdown-content { padding-top: 0 !important; }

  /* 页面级通用容器 */
  .page-container { position: relative; overflow-x: clip; }
  .page-container::before {
    content: ''; position: absolute; top: -150px; left: 50%; transform: translateX(-50%);
    width: 1200px; height: 500px; background-image: radial-gradient(circle, var(--sl-color-accent-low) 0%, transparent 70%);
    opacity: 0.25; filter: blur(120px); z-index: -1; pointer-events: none;
  }
  .divider-glow {
    height: 1px; width: 80%; max-width: 600px; margin: 0 auto;
    background: linear-gradient(90deg, transparent, var(--sl-color-accent), transparent); opacity: 0.3;
  }
  .content-wrapper { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem; }
  
  .aipali-section-title {
    font-size: 2.2rem; margin: 0 0 2.5rem 0; text-align: center;
    border: none !important; color: var(--sl-color-white); font-weight: 700;
  }

  /* Hero 布局 */
  .custom-hero-section {
    width: 100%; display: flex; justify-content: center;
    padding-top: 0; margin-top: -3.5rem; margin-bottom: 3rem;
  }
  .hero-container {
    max-width: 1200px; width: 100%; display: grid;
    grid-template-columns: 1fr auto; align-items: center;
    gap: 3rem; padding: 0 1.5rem; margin-inline: auto;
  }
  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.2rem); font-weight: 800; line-height: 1.1; margin: 0; white-space: nowrap; 
    background: linear-gradient(120deg, var(--sl-color-white) 30%, var(--sl-color-text-accent) 100%);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    text-shadow: 0 0 40px var(--sl-color-accent-low);
  }
  .hero-tagline { margin-top: 1.5rem; font-size: 1.25rem; color: var(--sl-color-gray-2); line-height: 1.6; max-width: 50ch; }
  .hero-actions { margin-top: 2.5rem; display: flex; gap: 1rem; flex-wrap: wrap; }
  
  .btn { padding: 0.8rem 1.6rem; border-radius: 99rem; font-weight: 600; text-decoration: none; transition: all 0.2s ease; font-size: 1rem; }
  
  html[data-theme='light'] .btn-primary { background-color: var(--sl-color-accent); color: #ffffff !important; }
  html[data-theme='light'] .btn-primary:hover { background-color: var(--sl-color-accent-high); color: #ffffff !important; }
  html[data-theme='dark'] .btn-primary { background-color: var(--sl-color-accent); color: #ffffff !important; }
  html[data-theme='dark'] .btn-primary:hover { background-color: var(--sl-color-accent-high); color: var(--sl-color-gray-6) !important; }
  
  .btn-secondary { border: 1px solid var(--sl-color-accent); color: var(--sl-color-white) !important; }
  .btn-secondary:hover { background: var(--sl-color-accent-low); }
  .btn-minimal { color: var(--sl-color-gray-2) !important; }
  .btn-minimal:hover { color: var(--sl-color-white) !important; text-decoration: underline; }

  .hero-image { display: flex; justify-content: flex-end; align-items: center; }
  .hero-image img { max-width: 320px; height: auto; transform: translate(0, -30px); background: transparent !important; }
  html[data-theme='light'] .img-dark { display: none; }
  html[data-theme='dark'] .img-light { display: none; }

  /* 离线控制中心 (同源 About.mdx 的玻璃态风格) */
  .offline-dashboard {
    width: 100%; margin: 0; padding: 0;
  }
  .aipali-glass-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem;
  }
  .aipali-glass-card {
    margin: 0 !important; height: 100%; display: flex; flex-direction: column; justify-content: center;
    border-radius: 1rem; border: 1px solid var(--sl-color-accent);
    background: linear-gradient(135deg, rgba(var(--sl-color-accent-low-rgb, 100,100,250), 0.1), transparent);
    backdrop-filter: blur(10px); transition: border-color 0.3s ease;
  }
  .dash-value { font-size: 2.8rem; font-weight: 700; color: var(--sl-color-text-accent); line-height: 1.2; }

  /* 操作按钮组 (规整统一) */
  .control-panel {
    display: flex; gap: 1.5rem; justify-content: center; flex-wrap: wrap; margin-bottom: 2.5rem;
  }
  .btn-action { 
    flex: 1; min-width: 220px; max-width: 280px; padding: 1rem 1.2rem; border-radius: 0.8rem; 
    font-weight: 600; cursor: pointer; font-size: 1.05rem; transition: all 0.2s ease; text-align: center;
  }
  .btn-action:disabled { opacity: 0.5; cursor: not-allowed; filter: grayscale(1); }
  .btn-action:not(:disabled):active { transform: scale(0.98); }
  
  .btn-primary-action { background: var(--sl-color-text-accent); color: var(--sl-color-black); border: none; }
  .btn-primary-action:hover:not(:disabled) { background: var(--sl-color-accent-high); box-shadow: 0 0 20px var(--sl-color-accent-low); }
  
  .btn-secondary-action { background: var(--sl-color-bg); color: var(--sl-color-white); border: 1px solid var(--sl-color-gray-4); }
  .btn-secondary-action:hover:not(:disabled) { background: var(--sl-color-gray-6); border-color: var(--sl-color-gray-2); }
  
  .btn-danger-action { background: var(--sl-color-bg); color: #ff5f56; border: 1px solid rgba(255, 95, 86, 0.4); }
  .btn-danger-action:hover:not(:disabled) { background: rgba(255, 95, 86, 0.1); border-color: #ff5f56; }

  /* 进度条 */
  .progress-container { margin-bottom: 2.5rem; opacity: 0; transition: opacity 0.3s ease; }
  .progress-container.visible { opacity: 1; }
  .progress-container.hidden { display: none; }
  .progress-info { display: flex; justify-content: space-between; font-size: 0.95rem; margin-bottom: 0.8rem; color: var(--sl-color-gray-2); font-family: monospace; }
  .progress-bar-bg { width: 100%; height: 6px; background: var(--sl-color-gray-5); border-radius: 3px; overflow: hidden; }
  .progress-bar-fill { height: 100%; width: 0%; background: var(--sl-color-text-accent); transition: width 0.3s ease; }

  /* 终端日志 (解决跳动与适配 Light Theme) */
  .terminal-window {
    background: var(--sl-color-bg-nav); border-radius: 0.8rem; overflow: hidden; border: 1px solid var(--sl-color-hairline);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    box-shadow: inset 0 2px 10px rgba(0,0,0,0.2);
  }
  .terminal-header {
    background: var(--sl-color-gray-6); padding: 12px 16px; border-bottom: 1px solid var(--sl-color-hairline); 
    display: flex; align-items: center;
  }
  .window-controls { display: flex; gap: 8px; }
  .dot { width: 12px; height: 12px; border-radius: 50%; }
  .dot-red { background: #ff5f56; } .dot-yellow { background: #ffbd2e; } .dot-green { background: #27c93f; }
  .terminal-title { margin-left: 15px; color: var(--sl-color-gray-3); font-size: 0.85rem; letter-spacing: 1px; text-transform: uppercase; }
  
  .terminal-body {
    margin: 0; padding: 1.2rem; height: 280px; overflow-y: auto; color: var(--sl-color-text-accent); font-size: 0.85rem; line-height: 1.6;
  }
  .terminal-body::-webkit-scrollbar { width: 6px; }
  .terminal-body::-webkit-scrollbar-track { background: transparent; }
  .terminal-body::-webkit-scrollbar-thumb { background: var(--sl-color-gray-5); border-radius: 3px; }
  
  .log-line {
    white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
  }

  /* 特性卡片复写 */
  .sl-card {
    background: linear-gradient(135deg, rgba(255,255,255,0.02), rgba(255,255,255,0)) !important;
    border: 1px solid var(--sl-color-hairline) !important; backdrop-filter: blur(10px);
  }
  .highlight-text {
    color: var(--sl-color-text-accent); background-color: var(--sl-color-accent-low);
    padding: 0.1em 0.4em; border-radius: 4px; font-weight: 600;
  }

  /* 响应式调整 */
  @media (max-width: 50rem) {
    .hero-container { grid-template-columns: 1fr; text-align: center; gap: 1.5rem; }
    .hero-image img { max-width: 300px; margin: 0 auto; transform: none; }
    .hero-title { white-space: normal !important; font-size: clamp(2rem, 8vw, 2.5rem); }
    .hero-tagline { margin-inline: auto; }
    .hero-actions { justify-content: center !important; margin-top: 1.5rem; }
    .aipali-glass-grid { grid-template-columns: 1fr; gap: 1rem; }
    .control-panel { flex-direction: column; }
    .btn-action { width: 100%; max-width: none; }
  }

  /* 页脚 */
  .aipali-custom-footer {
    margin-top: 6rem; padding: 4rem 1rem; border-top: 1px solid var(--sl-color-hairline);
    text-align: center; color: var(--sl-color-gray-3);
  }
  .footer-content { display: flex; flex-direction: column; align-items: center; gap: 0.3rem; }
  .footer-content p { margin: 0 !important; line-height: 1.6; }
  .brand { color: var(--sl-color-text-accent); font-weight: 600; }
  .footer-copyright a { color: var(--sl-color-text-accent); text-decoration: none; }
`}</style>