---
title: AIPali 智能化巴利三藏 | AI 问经
head:
  - tag: title
    content: "AIPali 智能化巴利三藏 | AI 问经"
description: 基于现代人工智能技术，重塑巴利三藏的阅读、检索与互动体验。
template: splash
---

<div className="chat-fullscreen-wrapper">
  <iframe
    className="fastgpt-iframe"
    src="https://ai.true-dhamma.com/chat/share?shareId=pKlTh130EtoSDdDoPOBjMxzy"
    allow="microphone *; *"
  />
</div>

<style>
  {`
    /* 1. 隐藏默认渲染的页面标题及锚点 */
    .sl-markdown-content > h1:first-child, #_top {
      display: none !important;
    }

    /* 2. 移除 Starlight 内容区域的默认外边距与内边距 */
    .sl-markdown-content {
      padding: 0 !important;
    }
    .content-panel {
      padding: 0 !important;
    }
    .sl-container {
      max-width: 100% !important;
      margin-inline: 0 !important;
      padding-inline: 0 !important;
    }

    /* 3. 外层容器：设置与下方一致的边距，并保持自适应 */
    .chat-fullscreen-wrapper {
      /* 【参数调节区】白边尺寸，默认 12px，可按需微调为 10px / 14px / 16px */
      --chat-gap: 12px;

      width: 100%;
      height: calc(100dvh - var(--sl-nav-height, 3.5rem));
      display: flex;
      box-sizing: border-box;

      /* 在顶部和底部加入一致的边距（若左右也需要边距，可改为：var(--chat-gap)） */
      padding: var(--chat-gap) 0;

      /* 背景底色：跟随 Starlight 的主题底色，浅色模式为白/灰底，深色模式为深底 */
      background: var(--sl-color-bg);
      overflow: hidden;
      position: relative;
    }

    /* 4. iframe 完整展示：恢复原始高度，文字居中不受影响 */
    .fastgpt-iframe {
      width: 100%;
      height: 100% !important;
      margin: 0 !important;
      border: none;
      display: block;
    }

    /* 5. 锁定外层页面滚动，交由聊天框内部自行滚动 */
    body {
      overflow-y: hidden;
    }
  `}
</style>