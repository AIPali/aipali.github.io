---
title: AIPali 智能化巴利三藏 | AI 问经
head:
  - tag: title
    content: "AIPali 智能化巴利三藏 | AI 问经"
description: 基于现代人工智能技术，重塑巴利三藏的阅读、检索与互动体验。
template: splash
---

<div className="chat-viewport">
  <div className="chat-card">
    <iframe
      src="https://ai.true-dhamma.com/chat/share?shareId=pKlTh130EtoSDdDoPOBjMxzy"
      allow="microphone *; *"
    />
  </div>
</div>

<style is:global>
  {`
    /* ============================================================
       1. 关键修复：穿透消除 Starlight 顶部及主面板的所有分界线与阴影
       ============================================================ */
    header, 
    .header, 
    .main-frame, 
    .main-pane,
    main {
      border-bottom: none !important;
      border-top: none !important;
      box-shadow: none !important;
      outline: none !important;
    }

    /* 将 Starlight 专门用于画线的变量设为透明（双重保险） */
    :root {
      --sl-color-hairline-shade: transparent !important;
    }

    /* ============================================================
       2. 重置 Starlight 正文内边距与默认标题
       ============================================================ */
    .sl-markdown-content > h1:first-child, #_top { 
      display: none !important; 
    }
    .sl-markdown-content, .content-panel { 
      padding: 0 !important; 
    }
    .sl-container { 
      max-width: 100% !important; 
      margin-inline: 0 !important; 
      padding-inline: 0 !important; 
    }
    body { 
      overflow-y: hidden; 
    }

    /* ============================================================
       3. 外层视口：顶部间距精准留白 3px
       ============================================================ */
    .chat-viewport {
      width: 100%;
      height: calc(100dvh - var(--sl-nav-height, 3.5rem));
      /* 顶部 3px，右/下/左 12px */
      padding: 3px 12px 12px 12px;
      box-sizing: border-box;
      background-color: var(--sl-color-bg);
      display: flex;
    }

    /* ============================================================
       4. 卡片容器：去掉顶部 border-top，避免在 3px 处显出线条
       ============================================================ */
    .chat-card {
      width: 100%;
      height: 100%;
      border-radius: 12px;
      overflow: hidden;
      /* 左右和底部保留细边框，顶部边框强制清空 */
      border: 1px solid var(--sl-color-hairline);
      border-top: none !important; 
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
      background: var(--sl-color-bg);
    }

    .chat-card iframe {
      width: 100%;
      height: 100%;
      border: none;
      display: block;
    }
  `}
</style>